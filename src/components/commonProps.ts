export interface LayoutProps {
    width?: number;
    height?: number;
    flex?: boolean | number;
}

export interface StyleProps {
    color?: string;
    style?: React.CSSProperties;
    className?: string;
}
